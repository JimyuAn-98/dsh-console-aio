# dsh-console-aio 运行依赖
# PySide6 为 GUI 框架(安装包会内置; 源码运行需 pip install -r requirements.txt)
pyside6>=6.6

# 用量统计解压 session.jsonl.zstd 用(缺失时用量页降级提示)
zstandard

# 开发/打包(可选):
#   pip install pyinstaller   # 运行 build_win.bat 打包单文件 exe + Inno 安装包
