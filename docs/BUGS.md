# 已知问题（BUGS）

> 状态：待修 / 已修复。新增问题时按编号追加；修复后在条目后标注修复提交。

| 编号 | 问题 | 状态 | 备注 |
|------|------|------|------|
| BUG-001 | 插件停用/启用不生效。原记录方向（bundle名 vs entry id 映射）此前已实现；实修根因：启用删空禁用行后 `write_yaml([])` 写出**空文件**，dsh patch 层解析为 null 拒绝加载 → 运行中 web HMR 失效（启用不生效），profile 重启即启动失败 | 已修复（2026-08-29） | write_yaml 空容器写 `[]`/`{}`；启用未命中禁用行时追加 `disabled: false` 强启用行（对齐 dshmarket enableRow）；本机 web profile 坏文件已修复。查证：启停链路无需 build（dsh plugin 为纯 pnpm 转发器）。排查记录见 `.agents/notes/implemented/bug-fix/2026-08-29-plugin-enable-empty-patch.md` |
| BUG-002 | `os.kill(pid, 0)` Windows 语义陷阱：signal 0 == CTRL_C_EVENT，实际是向共享控制台广播 Ctrl+C（非 Unix 探活）；在宿主 harness 伪控制台内执行会 SIGINT 掉宿主 web | 已修复（2026-08-29） | `core/tunnel_mgr._pid_alive` 改用 tasklist CSV；上游同一 bug 见 deepseek-ai/deepseek-harness Discussion #4713；排查记录见 `.agents/notes/implemented/bug-fix/2026-08-29-os-kill-ctrlc-harness.md` |
| BUG-003 | 版本页"更新后不重启"：源码模式重启指向不存在的 app_pyside.py（FileNotFoundError 被吞） | 已修复（阶段2 波1） | 迁移 version 页到 core 时修复 |
| BUG-004 | 会话归档调用不存在的 `dsh_data.write_workspace`（AttributeError，归档/恢复自迁移起失效） | 已修复（阶段2 波2） | core 信封直写 workspace.json |
| BUG-005 | 隧道"停止后又被自动重连"：persist 停止标志随页面重建丢失 | 已修复（阶段1） | 停止标志改由 service 持有 |
| BUG-006 | 隧道页"运行更新"按钮引用未定义的 `_run_update`（点击静默报错） | 已修复（阶段1） | 从 tkinter 旧主程序恢复完整更新流至 `dshctl.update_dsh` |
| BUG-007 | 部署快照"会话大小"恒 0：`DshRemote.dir_stats` 本地分支只数子目录直接文件，而会话目录是 组/会话/文件 三层 | 已修复（2026-08-29） | 改 `_tree_size` 有界递归；概览页/部署页快照同源修复 |
| BUG-008 | 概览页快照结果经 bridge 发进底部日志区，`dep_status` 永远停在"读取中…"（技术债"裸线程未走信号"的实际代价） | 已修复（2026-08-29） | 概览页重设计：页面级 Signal + safe_emit 回 UI，数据改走 service.ctl 探测 |
| BUG-010 | 页面导航重建导致进行中任务状态丢失：dsh 安装中切走再切回，进度条与页内日志清空（仅主控制台还在输出） | 已修复（2026-09-10） | MainWindow `_pages` 常驻页面实例 + `on_show` 钩子；DSH 管理页跨导航保留。见 `.agents/notes/implemented/bug-fix/2026-09-10-install-state-tunnel-card-uninstall.md` |
| BUG-011 | 卸载 dsh 删不净：源码目录在 C:\Users 下时 `.git/objects` 等只读文件触发 WinError 5 | 已修复（2026-09-11 加固） | `core/env.py::_rmtree_force`：清只读位 + Windows `\\?\` 长路径前缀 + 带兜底回调 rmtree + 3 次重试(0.4s 间隔) + `cmd rmdir /s /q` 兜底 + 删净存在性检查；全过程逐行落盘。二次加固（2026-09-11）：先原生 cmd rmdir 快删（junction 只删链接）→ Python 迭代式后序精修（清只读/跳 junction/每 2000 项进度日志）→ 残留清单。junction 环回归测试见 tests/test_core_rmtree_force.py。**实机验证通过（2026-09-11 23:36：75541 项 / 31.1s 删净）** |
| BUG-012 | 隧道误报绿灯：无隧道运行时仍显示在线（管理页卡片 + 右侧监控端口灯） | 已修复（2026-09-10） | 卡片与右栏端口灯统一以隧道进程存活为权威（`TunnelManager.running_map` + `port_states_from_running`）；反向隧道需进程存活且远端在听；右栏端口灯三态(未启动空心灰/在线绿/异常红) |
| BUG-009 | 打包运行读不到 config.json：core/config.py 内**后置的旧 `_default_config_path` 定义覆盖了文件顶部新别名**（Python 自上而下执行），打包时 `__file__` 在临时解压目录 → 读写全落临时目录，源码运行不受影响（旧定义恰好在仓库根） | 已修复（2026-08-30） | 删除旧定义，load/save 直连公开 `default_config_path()`（frozen→exe 目录）；设置页显示配置实际路径；离屏复现+字节码比对定位 |
| BUG-013 | 全局包模式安装失败：`pnpm add -g` 报 `The configured global bin directory "...\pnpm\bin\bin" is not in PATH` | 已修复（2026-09-11，待用户复测） | 根因：`core/pkgmgr.py::pnpm_env` 把 `PNPM_HOME` 设成了全局 bin 目录本身，而 pnpm 会在 `PNPM_HOME` 后再拼 `bin`。修复方向：不再设置 `PNPM_HOME`，只把解析出的全局 bin 目录前置进 `PATH`（若环境已有 `PNPM_HOME`，则用 `<PNPM_HOME>\bin`）。本机三组对照：A `PNPM_HOME=<...\pnpm\bin>` 复现；B 仅 PATH 前置、C `PNPM_HOME=<...\pnpm>` 均正常。**修复**：`pnpm_env` 不再设置 `PNPM_HOME`，只把 `<pnpm home>\bin` 前置进 PATH；新增 `pnpm_home()`，`global_bin_dir()` 回退同源。 |
| BUG-014 | 源码安装：目标目录已存在且非空但不是 dsh 仓库时仍跳过 clone，随后 git fetch 报 not a git repository | 已修复（2026-09-11，待用户复测） | core/env.py::install_dsh 的「目录已存在且有内容则跳过 clone」只看非空，未校验是否 dsh git 仓库，随后无条件按已 clone 处理。修复方向：跳过前校验 target/.git 与远程是否 dsh 仓库，否则中文报错提示换目录或先清空。**修复**：新增 `_is_dsh_checkout`（复用 `pkgmgr.source_info`），非 dsh 仓库直接中文报错返回，不再继续执行 git 命令。日志 install-20260911-234503.log |
| BUG-015 | 版本 tag 前缀未规范化：dsh Release tag 形如 dsh-v0.1.5-rc.2，包模式 install_cmd 只去单个 v 前缀会拼出非法 npm 版本 @deepseek-ai/dsh@dsh-v0.1.5-rc.2 | 已修复（2026-09-11，待用户复测） | core/pkgmgr.py::install_cmd 与 core/dshctl.py::_deploy_pkg_version 仅 strip 单个 v/@。修复方向：统一版本规范化（dsh-v / v / 裸版本 -> npm 版本 0.1.5-rc.2），源码模式仍用原始 tag（git checkout 需要 dsh-v0.1.5-rc.2）。git tag 实测：dsh-v0.1.1..dsh-v0.1.5-rc.2。**修复**：新增 `pkgmgr.npm_version()` 作为唯一规范化实现（`dsh-v`/`v`/`@`/裸版本），`install_cmd`、`_deploy_pkg_version`、`fetch_dsh_releases` 三处统一改用它；源码模式仍用原始 tag |
| BUG-016 | 包模式启动失败：dsh 运行时插件全部 ERR_MODULE_NOT_FOUND（Cannot find package 各种 @deepseek-ai/dsh-*），解析路径都在 pnpm 全局虚拟仓库 pnpm/store/v11/links | 已修复（2026-09-11，待用户复测） | **修复**：包模式从 pnpm 改用 npm（`npm install -g` / `npm install -g @latest` / `npm uninstall -g`，启动用 npm 全局前缀下 `dsh.cmd`）——npm 扁平布局与官方 npx 同源，插件可解析。根因：dsh 的 cordis-plugin-loader 运行时动态 import 插件包，Node 只从 loader 目录逐级向上找 node_modules；pnpm 隔离式布局（全局虚拟仓库）把插件放在 @deepseek-ai/dsh/node_modules 下，不在 loader 祖先链上 → 100+ 条模块找不到。官方只文档化 npx @deepseek-ai/dsh web（npm 扁平布局）。控制台只负责起 dsh web，非控制流 bug，但“包模式用 pnpm -g”不成立。候选修法：A) 包模式改 npm 全局（npm i -g / npm uninstall -g / 启动用 npm prefix 下 shim）；B) 包模式改官方 npx @deepseek-ai/dsh@<ver> web；C) pnpm 强制 node-linker=hoisted（需实测）。证据：dsh-web.err.log 3461 行。**已确认**：操作日志 dsh-20260912-002527.log 第 2 行 `[模式] 全局包模式: ...\pnpm\bin\dsh.cmd web`、第 3465 行 `[dsh-web] 启动失败: 进程已异常退出 (退出码 1)`——包模式启动本身正常，是 dsh 因插件解析失败自己退出。 |
| BUG-017 | npm 全局安装/更新期间界面基本无输出（实测约 4 分钟静默），用户无法判断是否卡死 | 已修复（2026-09-12，待用户复测） | npm 在非 TTY(管道) 下会抑制进度输出，且依赖解析阶段本身静默；install_dsh_pkg / update_dsh_pkg 走 stream_cmd 只能收到零星行(安装完才一起出来)。修复方向：长命令加心跳(每 15s 打 elapsed + 当前阶段, 复用删除快删的做法)；npm 加 --loglevel=http 暴露下载行, --no-fund --no-audit 减噪, 更新加 --prefer-offline。**修复**：`DshCtl.stream_cmd` 改为独立读线程 + 队列 + 主循环每 0.5s 醒（长命令静默期间每 15s 打 `... 已运行 N 秒(命令仍在执行)`；顺带修掉此前直接在 `stdout.readline()` 阻塞导致**静默命令超时判断失效**的隐患）；`pkgmgr` 的 npm 命令统一加 `--loglevel=http --no-fund --no-audit --prefer-offline`。新增 `tests/test_core.py::TestStreamCmdHeartbeat`(心跳 + 超时) |
| BUG-018 | 安装/卸载 dsh 后控制台配置不生效，必须去设置页点保存；更新按钮/版本卡「部署此版本」因此误走源码分支并提示仓库不存在 | 已修复（2026-09-12，待用户复测） | 根因：ui/pages_dsh 的安装/卸载/部署/更新成功回调未调 app.reload_config()，DshService._cfg（派生 dict）仍是旧值，detect_mode / update_dsh / deploy_dsh_version 全读 stale 配置；且更新/部署确认文案写死为 git 构建步骤。修复：新增 _reload_app_config()，在安装/卸载/部署/更新成功后调用并重新 _detect_mode()；更新/部署确认文案随模式（package = npm install -g @deepseek-ai/dsh@版本）。 |
| BUG-019 | 安装/更新失败时日志数千行、根因不突出；且全局 npm 命令继承控制台启动目录(cwd=dsh-new) | 已修复（2026-09-12） | 起因：实机更新遇 npm ETARGET（@deepseek-ai/dsh-tool-subagent-report@^0.1.0-rc.8 当时镜像未同步，属瞬时/上游，本机 --dry-run 现已可解析 518 包）。改进：stream_cmd 收集错误行，失败时打 [失败摘要]（末 5 条）+ ETARGET/notarget 时提示「镜像未同步或版本未发布，可稍后重试或指定版本」；pkgmgr 新增 npm_cwd()（用户主目录），四处全局 npm 调用不再继承控制台 cwd。 |
| BUG-020 | 更新报 ETARGET（No matching version found for 某子包@^0.1.0-rc.8），但镜像/官方 registry 实际已有该版本；重试依旧 | 已修复（2026-09-12，待用户复测） | 根因：BUG-017 给 npm 命令加的 --prefer-offline 会**跳过缓存元数据新鲜度校验**，npm 复用本机过期的 packument，于是已发布版本被判为不存在。实机核对：0.1.0-rc.8 于 2026-08-19 已发布，npmmirror 与官方均有；本机 --dry-run 加 --prefer-online 可解析 518 包。修复：_NPM_FLAGS 改为 --prefer-online（并保留 --loglevel=http/--no-fund/--no-audit）。 |
