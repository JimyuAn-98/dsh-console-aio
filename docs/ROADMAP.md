# 路线图（ROADMAP）

> 当前进展与未来计划的状态总览。
> **历史路线**见 `docs/archive/ROADMAP_HISTORY.md`；**发版叙述**见 `RELEASE_NOTES.md`；
> **愿景探索**见 `docs/VISION_部署子工具组.md`；**早期方案归档**见 `docs/archive/PLANS.md`；
> **已知问题**见 `docs/BUGS.md`；**实施计划索引**见 `docs/plans/README.md`；
> **架构唯一权威**见 `docs/ARCHITECTURE.md`。

## 当前状态（2026-09-11）

### 最近批次

- ✅ **长操作完整输出日志 + 卸载删除加固**（2026-09-11，未发版）：安装/更新/卸载/部署/启停/批量隧道/通用命令全过程落盘 `<临时目录>/dsh-console-ops/<op>-<时间戳>.log`（DSH 管理页「打开操作日志」；落盘前对 `token=`/`Bearer` 脱敏）；`_rmtree_force` 加长路径前缀 + 3 次重试 + `cmd rmdir` 兜底 + 逐行删除日志。单测 `tests/test_core_rmtree_force.py`、`tests/test_service_oplog.py`。
- ✅ **节点访问规划·阶段 3**（2026-09-11，未发版）：`core/nodeid.py`（节点码）+ `core/runtime.py`（runtime.json 落盘/公网信箱 JSON/Token 三级解析）；捕获即投递，`_sync_push_token` 改用 `node_id`；设置页「本机节点」卡（节点码/重新生成/立即同步）+ 部署页「从公网信箱发现」（绑定/删除条目）。**规划四阶段全部落地。**
- ✅ **节点访问规划·阶段 2**（2026-09-11，未发版）：`deployments[]` 显式化 `node_key/web_port/tunnel_id/access_port` + 节点添加/编辑对话框；访问端口推导收口 `deployment_access_port`；部署页「编辑节点」「在浏览器打开」；设置页移除遗留 `lab_name`（命名单一来源见 ARCHITECTURE §3.1）。
- ✅ **节点访问规划·阶段 1**（2026-09-11，未发版）：总览删「隧道状态」死卡、"部署"区改名"节点"、远程节点改经隧道端口探活、新增「在浏览器打开」；规划见 `docs/plans/20260911-节点访问规划-v1.md`。
- ✅ **实测三处问题修复**（2026-09-10，未发版）：安装进度跨页丢失（常驻页面实例 + on_show）、卸载残留只读 `.git`（`_rmtree_force`）、隧道卡与右侧监控端口灯统一（改判隧道进程存活）；右栏未启动态改空心灰点（三态一致）。
- ✅ **缓存编排收口：种类注册表 + CacheableMixin**（2026-09-10，未发版）：`core/cache.py` 加种类注册表与管理 API；新增 `ui/cacheable.py`，6 个数据页迁移去重。
- ✅ **总览页实时状态缓存过期与缓存防重入修复**（2026-09-10，未发版）：命中缓存补轻量 socket 探活刷新运行状态卡；`force` 不再绕过 busy 防重入。
- ✅ **DSH 管理页部署指定版本与版本固定**（2026-09-10，未发版）：按 Release tag 切换/回退并重建（脏工作区确认、旧版回退备份提示、固定后更新弹窗确认切回主线）；`config.dsh_version_pin` 记录固定；安装卡支持指定版本；更新/部署加 step 进度条。
- ✅ **DSH 管理页版本发布日志查看**（2026-09-10，未发版）：版本信息卡改为单栏「版本下拉 + 中文更新日志」，数据源 GitHub Releases + 会话内 TTL 缓存；新增 core 纯函数与单测。
- ✅ **安装版一键更新升级体验**（2026-09-10）：一键更新改为下载最新安装包 → 自动退出 → 运行安装器（流式进度、MZ 防呆、SHA256 尽力校验）。
- 🚀 **v0.8.0 发布**（2026-09-10）：累积 09-03 隧道治理与 09-10 启动/插件两批；tag `v0.8.0` → CI 自动出安装包。
- ✅ **dsh 启动报错捕获、插件管理 Profile 死锁修复与插件更新能力**（2026-09-10）：
  - `core/dshctl.py` 引入增量 tail 与启动前端口预检：拉起新进程前主动探测目标端口并清理旧残留进程，防止端口被占导致的秒级假就绪；
  - 启动子进程后 15s 初始存活与错误流式观测 + 后台持续 15s 监视器（去除 Token 提前退出限制），进程崩溃时把 err.log 堆栈打入底部控制台并标记卡片离线；
  - 强化 `stop_dsh`（按端口精准 kill + 扩宽 node 匹配）；`update_dsh` 校验重启返回；修正 `run_dsh("restart")` 时序；补 service `stop_dsh/restart_dsh` 并修正托盘接线；
  - 插件管理页修复 `_apply_profiles` busy 死锁，新增【更新】/【更新全部插件】与安装输入；
  - 新增 10 个单元测试。决策记录：`.agents/notes/implemented/bug-fix/2026-09-10-dsh-start-log-capture-and-plugin-profile-hang.md`。
- ✅ **SSH 隧道生命周期治理、动态监控修复与自检重构**（2026-09-03）：单一事实源（`derived` 由当前方案动态合成端口）、方案切换自动停旧隧道、下拉框陈旧缓存修复、自检三态（🟢在线/⚪未启动/🔴异常）重构、设置页旧隧道字段清理。详见 `RELEASE_NOTES.md`。
- ✅ **通用动态 SSH 隧道管理架构 + 场景向导**（2026-09-02）：声明式 `TunnelItem` 模型（任意数量/正反向/多端口/断线重连）、旧配置自动迁移、4 大场景向导、动态卡片流、右栏与托盘联动。
- ✅ **dsh 0.1.2+ 鉴权 Token 静默捕获与 SSH 信箱跨端免密**（2026-09-02）：启动时正则捕获 Token、反向隧道同步至 `~/.dsh_runtime/<node>.token`（600）、拼装免密链接。
- ✅ **09-01 批次**：主入口模块化拆分（17 页全独立 `pages_*.py`）、全页内联确认条 `ConfirmBanner`、系统托盘常驻、通用缓存层推广与技术债收口。详见 `docs/archive/ROADMAP_HISTORY.md`。

### 待办与候选（已按代码核实，2026-09-10）

**未做：**

- **卸载删净实测（BUG-011）**：`_rmtree_force` 已加固（长路径前缀/重试/`cmd rmdir` 兜底 + 逐行日志），待实机在 `C:\Users` 下的 dsh 源码环境复测确认（只读 `.git` 场景）；若仍失败，操作日志会逐条列出删不掉的路径。
- **布局记忆**：分栏尺寸/收起状态持久化（全仓库无 `saveState/restoreState`，主分栏 `setSizes([172,700])` 写死）。
- **弹窗收敛剩余（4 处）**：`ui/dialog_tunnel_wizard.py`、`ui/pages_tunnels.py`（2 处）、`ui/pages_ops.py` 仍使用 `QMessageBox.question`，与「全页内联确认」目标不符。
- **主题文件跨机导出/导入**：`themes/*.json` 目前仅本机保存/加载，无打包导出。
- **小白引导**：首次使用向导 / 诊断助手 / 常见问题。
- **远程部署子工具组**：延后/独立试验（独立分支 + WSL 原型，验证后再合并；提案见 `.agents/notes/proposed/feature/2026-08-30-remote-deploy.md`）。
- **跨平台 core 平台层**：UI 层已按「一套设计系统 + 平台适配层」就绪，core 层 Windows 优先，等有 Linux/macOS 用户再抽。

**已决策不做（保留理由）：**

- **运行中切换 Mica/纯色材质**：窗口材质须在显示前设置，运行中切换需重建整窗（挂 service 桥/监控线程/全部页面），风险大于收益；透明度调 rgba token 即可。

**校正（先前误列候选，实为已完成）：**

- ✅ **多套拓扑配置切换**：`core/tunnel_planner.py` + 隧道页「隧道方案」卡（快照保存/切换/校验/自检）。
- ✅ **主题预设**：明/暗变体 + 主题文件 `themes/*.json` 存取 + 全部颜色实时可调。
- ✅ **托盘常驻、技术债收口、弹窗收敛主体**：均已落地（详见 `docs/archive/ROADMAP_HISTORY.md` 与 09-01 批次）。

## 路线表

| 阶段 | 内容 | 状态 |
|------|------|------|
| **P0 配置驱动** | 三处命名 + 监测端口 GUI 编辑 + 配置热重载 | ✅ 完成 |
| **P1 外观** | 亚克力/原生标题栏/分栏/右栏动画/品牌 + 表格页多栏展开 | ✅ 完成 |
| **P2 功能对齐** | 插件管理对齐 dsh web、日志查看器 | ✅ 完成 |
| **P3 进阶** | 命令面板、配置导出/导入、诊断报告、数据可视化 | ✅ 完成 |
| **P4 愿景主线** | 隧道规划器（映射/冲突/隧道组、多方案快照） | ✅ 完成 |
| **P4 愿景主线** | 远程部署子工具组 | ⏸ 延后/独立试验 |
| **P5 逐功能校验** | 17 页逐页校验与完善优化（当前阶段） | ⏳ 进行中 |

## 索引

- 架构唯一权威：`docs/ARCHITECTURE.md`
- 测试分层与安全边界：`docs/TESTING.md`
- 功能校验矩阵（P5 进度）：`docs/FEATURE_AUDIT.md`
- 实施计划沉淀与索引：`docs/plans/README.md`
- 历史路线归档：`docs/archive/ROADMAP_HISTORY.md`
- 历史方案归档：`docs/archive/`
- Agent 决策记录：`.agents/notes/`
