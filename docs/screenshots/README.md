# 截图清单

README 引用了以下截图，按文件名把截图放到本目录即可（自动生效）。

| 文件名 | 内容 | 截图要点 |
|--------|------|---------|
| main.png | 主界面全貌 | 顶部工具栏（含"dsh 管理"菜单）+ 隧道卡片 + 健康监控两行 + 日志区 |
| install-dialog.png | 安装 dsh 向导 | 仓库地址/目标目录输入框 + 环境预检结果 + 浏览按钮 |
| env-check.png | 环境检查窗口 | git/node/npm/pnpm 四行：版本/推荐基准/状态 + 更新/安装/卸载按钮 |
| config-wizard.png | 配置向导 | 分组表单（服务器/本机 dsh/隧道/轮询）+ 模板下拉 |
| mgmt-menu.png | dsh 管理菜单展开 | 顶部菜单下拉展开显示 9 个菜单项 |
| sessions.png | 会话与工作区 | 左侧分组树 + 右侧会话详情 + 归档标记 |
| plugins.png | 插件管理 | profile 下拉 + 插件表（状态/名称/版本/来源）+ 安装输入框 |
| usage.png | 模型用量统计 | 按模型表（token/调用/费用）+ 按天表 |
| llm.png | LLM 配置 | 默认模型下拉 + 自定义 provider 表 + 密钥提示 |

## 建议
- PNG 或 JPG 均可；推荐窗口尺寸截全（不要拉伸）
- 敏感信息注意：截图不要包含真实 IP/用户名（可先用配置向导改成占位符再截，或截后打码）
- 若某张暂时不截，README 里对应图片会显示 broken——截好后放进来即可
